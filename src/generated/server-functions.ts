// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  ClientType,
  ClientCreateInput,
  ClientUpdateInput,
  ProjectType,
  ProjectCreateInput,
  ProjectUpdateInput,
  TaskType,
  TaskCreateInput,
  TaskUpdateInput,
  MeetingType,
  MeetingCreateInput,
  MeetingUpdateInput,
  ContactType,
  ContactCreateInput,
  ContactUpdateInput,
  MilestoneType,
  MilestoneCreateInput,
  MilestoneUpdateInput,
  TagType,
  TagCreateInput,
  TagUpdateInput,
  FileType,
  FileCreateInput,
  FileUpdateInput,
  ActivityLogType,
  ActivityLogCreateInput,
  ActivityLogUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// Client Client Functions
// ============================================================================

/**
 * List all Client records
 */
export async function getClientList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ClientType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/clients${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Client')
  }
  const json = await response.json()
  return (json.items || []) as ClientType[]
}

/**
 * Get a single Client by ID
 */
export async function getClientById(args: { data: { id: string; userId?: string } }): Promise<ClientType> {
  const url = `${getApiBase()}/api/clients/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Client not found')
  }
  const json = await response.json()
  return json.data as ClientType
}

/**
 * Create a new Client
 */
export async function createClient(args: { data: { input: ClientCreateInput; userId?: string } }): Promise<ClientType> {
  const url = `${getApiBase()}/api/clients`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Client')
  }
  const json = await response.json()
  return json.data as ClientType
}

/**
 * Update an existing Client
 */
export async function updateClient(args: { data: { id: string; input: ClientUpdateInput; userId?: string } }): Promise<ClientType> {
  const url = `${getApiBase()}/api/clients/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Client')
  }
  const json = await response.json()
  return json.data as ClientType
}

/**
 * Delete a Client
 */
export async function deleteClient(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/clients/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Client')
  }
  return { success: true }
}

// ============================================================================
// Project Client Functions
// ============================================================================

/**
 * List all Project records
 */
export async function getProjectList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ProjectType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/projects${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Project')
  }
  const json = await response.json()
  return (json.items || []) as ProjectType[]
}

/**
 * Get a single Project by ID
 */
export async function getProjectById(args: { data: { id: string; userId?: string } }): Promise<ProjectType> {
  const url = `${getApiBase()}/api/projects/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Project not found')
  }
  const json = await response.json()
  return json.data as ProjectType
}

/**
 * Create a new Project
 */
export async function createProject(args: { data: { input: ProjectCreateInput; userId?: string } }): Promise<ProjectType> {
  const url = `${getApiBase()}/api/projects`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Project')
  }
  const json = await response.json()
  return json.data as ProjectType
}

/**
 * Update an existing Project
 */
export async function updateProject(args: { data: { id: string; input: ProjectUpdateInput; userId?: string } }): Promise<ProjectType> {
  const url = `${getApiBase()}/api/projects/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Project')
  }
  const json = await response.json()
  return json.data as ProjectType
}

/**
 * Delete a Project
 */
export async function deleteProject(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/projects/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Project')
  }
  return { success: true }
}

// ============================================================================
// Task Client Functions
// ============================================================================

/**
 * List all Task records
 */
export async function getTaskList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<TaskType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/tasks${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Task')
  }
  const json = await response.json()
  return (json.items || []) as TaskType[]
}

/**
 * Get a single Task by ID
 */
export async function getTaskById(args: { data: { id: string; userId?: string } }): Promise<TaskType> {
  const url = `${getApiBase()}/api/tasks/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Task not found')
  }
  const json = await response.json()
  return json.data as TaskType
}

/**
 * Create a new Task
 */
export async function createTask(args: { data: { input: TaskCreateInput; userId?: string } }): Promise<TaskType> {
  const url = `${getApiBase()}/api/tasks`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Task')
  }
  const json = await response.json()
  return json.data as TaskType
}

/**
 * Update an existing Task
 */
export async function updateTask(args: { data: { id: string; input: TaskUpdateInput; userId?: string } }): Promise<TaskType> {
  const url = `${getApiBase()}/api/tasks/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Task')
  }
  const json = await response.json()
  return json.data as TaskType
}

/**
 * Delete a Task
 */
export async function deleteTask(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/tasks/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Task')
  }
  return { success: true }
}

// ============================================================================
// Meeting Client Functions
// ============================================================================

/**
 * List all Meeting records
 */
export async function getMeetingList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<MeetingType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/meetings${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Meeting')
  }
  const json = await response.json()
  return (json.items || []) as MeetingType[]
}

/**
 * Get a single Meeting by ID
 */
export async function getMeetingById(args: { data: { id: string; userId?: string } }): Promise<MeetingType> {
  const url = `${getApiBase()}/api/meetings/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Meeting not found')
  }
  const json = await response.json()
  return json.data as MeetingType
}

/**
 * Create a new Meeting
 */
export async function createMeeting(args: { data: { input: MeetingCreateInput; userId?: string } }): Promise<MeetingType> {
  const url = `${getApiBase()}/api/meetings`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Meeting')
  }
  const json = await response.json()
  return json.data as MeetingType
}

/**
 * Update an existing Meeting
 */
export async function updateMeeting(args: { data: { id: string; input: MeetingUpdateInput; userId?: string } }): Promise<MeetingType> {
  const url = `${getApiBase()}/api/meetings/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Meeting')
  }
  const json = await response.json()
  return json.data as MeetingType
}

/**
 * Delete a Meeting
 */
export async function deleteMeeting(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/meetings/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Meeting')
  }
  return { success: true }
}

// ============================================================================
// Contact Client Functions
// ============================================================================

/**
 * List all Contact records
 */
export async function getContactList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ContactType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/contacts${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Contact')
  }
  const json = await response.json()
  return (json.items || []) as ContactType[]
}

/**
 * Get a single Contact by ID
 */
export async function getContactById(args: { data: { id: string; userId?: string } }): Promise<ContactType> {
  const url = `${getApiBase()}/api/contacts/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Contact not found')
  }
  const json = await response.json()
  return json.data as ContactType
}

/**
 * Create a new Contact
 */
export async function createContact(args: { data: { input: ContactCreateInput; userId?: string } }): Promise<ContactType> {
  const url = `${getApiBase()}/api/contacts`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Contact')
  }
  const json = await response.json()
  return json.data as ContactType
}

/**
 * Update an existing Contact
 */
export async function updateContact(args: { data: { id: string; input: ContactUpdateInput; userId?: string } }): Promise<ContactType> {
  const url = `${getApiBase()}/api/contacts/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Contact')
  }
  const json = await response.json()
  return json.data as ContactType
}

/**
 * Delete a Contact
 */
export async function deleteContact(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/contacts/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Contact')
  }
  return { success: true }
}

// ============================================================================
// Milestone Client Functions
// ============================================================================

/**
 * List all Milestone records
 */
export async function getMilestoneList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<MilestoneType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/milestones${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Milestone')
  }
  const json = await response.json()
  return (json.items || []) as MilestoneType[]
}

/**
 * Get a single Milestone by ID
 */
export async function getMilestoneById(args: { data: { id: string; userId?: string } }): Promise<MilestoneType> {
  const url = `${getApiBase()}/api/milestones/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Milestone not found')
  }
  const json = await response.json()
  return json.data as MilestoneType
}

/**
 * Create a new Milestone
 */
export async function createMilestone(args: { data: { input: MilestoneCreateInput; userId?: string } }): Promise<MilestoneType> {
  const url = `${getApiBase()}/api/milestones`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Milestone')
  }
  const json = await response.json()
  return json.data as MilestoneType
}

/**
 * Update an existing Milestone
 */
export async function updateMilestone(args: { data: { id: string; input: MilestoneUpdateInput; userId?: string } }): Promise<MilestoneType> {
  const url = `${getApiBase()}/api/milestones/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Milestone')
  }
  const json = await response.json()
  return json.data as MilestoneType
}

/**
 * Delete a Milestone
 */
export async function deleteMilestone(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/milestones/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Milestone')
  }
  return { success: true }
}

// ============================================================================
// Tag Client Functions
// ============================================================================

/**
 * List all Tag records
 */
export async function getTagList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<TagType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/tags${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Tag')
  }
  const json = await response.json()
  return (json.items || []) as TagType[]
}

/**
 * Get a single Tag by ID
 */
export async function getTagById(args: { data: { id: string; userId?: string } }): Promise<TagType> {
  const url = `${getApiBase()}/api/tags/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Tag not found')
  }
  const json = await response.json()
  return json.data as TagType
}

/**
 * Create a new Tag
 */
export async function createTag(args: { data: { input: TagCreateInput; userId?: string } }): Promise<TagType> {
  const url = `${getApiBase()}/api/tags`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Tag')
  }
  const json = await response.json()
  return json.data as TagType
}

/**
 * Update an existing Tag
 */
export async function updateTag(args: { data: { id: string; input: TagUpdateInput; userId?: string } }): Promise<TagType> {
  const url = `${getApiBase()}/api/tags/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Tag')
  }
  const json = await response.json()
  return json.data as TagType
}

/**
 * Delete a Tag
 */
export async function deleteTag(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/tags/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Tag')
  }
  return { success: true }
}

// ============================================================================
// File Client Functions
// ============================================================================

/**
 * List all File records
 */
export async function getFileList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<FileType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/files${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list File')
  }
  const json = await response.json()
  return (json.items || []) as FileType[]
}

/**
 * Get a single File by ID
 */
export async function getFileById(args: { data: { id: string; userId?: string } }): Promise<FileType> {
  const url = `${getApiBase()}/api/files/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'File not found')
  }
  const json = await response.json()
  return json.data as FileType
}

/**
 * Create a new File
 */
export async function createFile(args: { data: { input: FileCreateInput; userId?: string } }): Promise<FileType> {
  const url = `${getApiBase()}/api/files`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create File')
  }
  const json = await response.json()
  return json.data as FileType
}

/**
 * Update an existing File
 */
export async function updateFile(args: { data: { id: string; input: FileUpdateInput; userId?: string } }): Promise<FileType> {
  const url = `${getApiBase()}/api/files/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update File')
  }
  const json = await response.json()
  return json.data as FileType
}

/**
 * Delete a File
 */
export async function deleteFile(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/files/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete File')
  }
  return { success: true }
}

// ============================================================================
// ActivityLog Client Functions
// ============================================================================

/**
 * List all ActivityLog records
 */
export async function getActivityLogList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ActivityLogType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/activity-logs${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ActivityLog')
  }
  const json = await response.json()
  return (json.items || []) as ActivityLogType[]
}

/**
 * Get a single ActivityLog by ID
 */
export async function getActivityLogById(args: { data: { id: string; userId?: string } }): Promise<ActivityLogType> {
  const url = `${getApiBase()}/api/activity-logs/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ActivityLog not found')
  }
  const json = await response.json()
  return json.data as ActivityLogType
}

/**
 * Create a new ActivityLog
 */
export async function createActivityLog(args: { data: { input: ActivityLogCreateInput; userId?: string } }): Promise<ActivityLogType> {
  const url = `${getApiBase()}/api/activity-logs`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ActivityLog')
  }
  const json = await response.json()
  return json.data as ActivityLogType
}

/**
 * Update an existing ActivityLog
 */
export async function updateActivityLog(args: { data: { id: string; input: ActivityLogUpdateInput; userId?: string } }): Promise<ActivityLogType> {
  const url = `${getApiBase()}/api/activity-logs/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ActivityLog')
  }
  const json = await response.json()
  return json.data as ActivityLogType
}

/**
 * Delete a ActivityLog
 */
export async function deleteActivityLog(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/activity-logs/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ActivityLog')
  }
  return { success: true }
}
